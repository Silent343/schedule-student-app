/** The four answer slots a quiz question offers, mirroring the original a/b/c/d. */
export type OptionKey = 'a' | 'b' | 'c' | 'd';

export const OPTION_KEYS: ReadonlyArray<OptionKey> = ['a', 'b', 'c', 'd'];
