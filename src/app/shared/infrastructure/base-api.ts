/**
 * Marker base class for bounded-context infrastructure API facades.
 * Subclasses orchestrate one or more `BaseApiEndpoint` instances and expose a
 * coarse-grained interface to the application layer (stores).
 */
export abstract class BaseApi {}
